import Auth from "../components/Auth";
import Wrapper from "../components/common/Wrapper";

const Login = () => {
  return (
    <Wrapper>
      <Auth />
    </Wrapper>
  );
};

export default Login;
